node_modules/
dist/
.env
.DS_Store
Thumbs.db
.playwright-mcp/
# Capturas locales de la home (no son assets del sitio)
home-top.png
nav-home-top.png
# Vinculo local con el proyecto de Vercel (IDs de org/proyecto, los genera
# `vercel link`). La configuracion que SI va versionada es app/vercel.json.
.vercel/
# Fotos stock de referencia (no se usan en el sitio)
fotos-stock/
# Scripts de debug puntuales
app/debug-*.mjs
app/debug-*.png
# Capturas de debug de Playwright (app/app/ es un typo del script, no parte
# del proyecto — debería ir a /tmp pero se quedó aquí)
app/app/
# Capturas estáticas de dev-mode (frames de Figma, no son assets del sitio)
app/final-*.png
# Auditorías locales de QA (Playwright, screenshots, console.log). Son
# output de sesiones puntuales — los .cjs de script y los logs podrían
# quedarse aquí si la auditoría encuentra un bug reproducible.
app/audit/

# Entrega de fotos y videos del cliente (agosto 2026). 3,4 GB de masters de
# camara: son la FUENTE, no assets de web. Lo que se publica vive ya
# procesado en app/public/. Meter esto en git haria el repo inclonable.
FOTO PARA NUEVA WEB 2026/
