# Produccion. Mismo Odoo que ya sirve el checkout de Eclipse
# (form_checkout.html apunta aqui desde siempre).
#
# ⚠️ CONFIRMAR antes de publicar: si Hispaniola va a tener su propio Odoo o su
# propio subdominio, se cambia aqui Y en Allowed origins (CORS) del modulo.
VITE_API_URL=https://sistemashispaniola.com
